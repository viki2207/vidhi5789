export default function hello3(msg) {
  console.log(msg);
}

//will allow you the access to other by importing it.
// to a variable as well
export let num = 1000;

//instead of this we are using end of file export{hello3, num} when we are not using for each function export
